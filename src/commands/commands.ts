// FunctionFile stub. The manifest requires <FunctionFile> so PowerPoint has
// a place to invoke ribbon-button actions, but our ShowTaskpane action does
// not need any function here yet.
void Office.onReady(() => {
  // intentionally empty
});
