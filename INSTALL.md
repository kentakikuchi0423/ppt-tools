# ppt-tools インストールガイド

PowerPoint で図形を **「揃える」「詰める」「位置入替」** を 1 クリックで実行できる無料アドインです。

このガイドは **PowerPoint で使うだけの方** 向けの手順です。プログラミング知識は不要、Node.js のインストールも不要です。所要時間は 10 分程度（ファイル共有設定を含む）。

> 自分で改造したい・コードを読みたい方は [`README.md`](./README.md) を参照してください。

## できること

- ⬇️⬆️⬅️➡️ 選択した図形を下／上／左／右に **詰める**（隙間ゼロで集約）
- 📏 選択した図形の **高さ・幅を揃える**（最後に選んだ図形が基準）
- 🔄 2 つの図形の **位置を入れ替える**

すべて **Ctrl+Z** で 1 回戻せます。

## 動作環境

- Windows 10 / 11
- **Microsoft 365 版 PowerPoint**（サブスクリプション版）を推奨。Office 2019 / 2021 などの永続ライセンス版でも動く可能性が高いですが、未検証です。
- インターネット接続（初回ロード時のみ。2 回目以降はオフラインでも操作可能）

Mac 版・Web 版 PowerPoint も理論上は動作しますが、本ガイドは Windows 向けです。

## インストール手順（5 ステップ・約 10 分）

### ステップ 1: マニフェストファイルをダウンロード

下のリンクを **右クリック → 「名前を付けてリンク先を保存」**：

👉 [https://kentakikuchi0423.github.io/ppt-tools/manifest.xml](https://kentakikuchi0423.github.io/ppt-tools/manifest.xml)

ファイル名は `manifest.xml` のままにしてください。

### ステップ 2: 専用フォルダを作って **ネットワーク共有** にする

> ⚠️ **重要**: PowerPoint のトラスト センターは **`C:\...` のようなローカルパスを受け付けません**（「https:// を指定しましたか？」というエラーになります）。同じ PC 内にある場合でも、Windows のファイル共有機能で **ネットワーク共有パス（UNC パス、`\\<コンピューター名>\...`）** に変える必要があります。
>
> これは Microsoft の公式手順です（[Sideload Office Add-ins for testing from a network share](https://learn.microsoft.com/en-us/office/dev/add-ins/testing/create-a-network-shared-folder-catalog-for-task-pane-and-content-add-ins)）。

#### 2-1: フォルダを作成して `manifest.xml` を入れる

エクスプローラーで好きな場所にフォルダを作成します。例: `C:\OfficeAddins\ppt-tools\`

ステップ 1 でダウンロードした `manifest.xml` をそのフォルダに移動します。

> このフォルダはアドインを使う間ずっと存在し続ける必要があります。ダウンロードフォルダや一時フォルダではなく、消さない場所を選んでください。

#### 2-2: フォルダを共有する

1. 作成したフォルダを **右クリック**
2. （Windows 11 のみ）**「その他のオプションを表示」**
3. **「プロパティ」** をクリック
4. **「共有」タブ** に切り替え
5. **「共有」** ボタンを押す
6. ドロップダウンから **現在ログインしているユーザー名** を選択 → **「追加」** ボタン
7. アクセス許可レベルは **「読み取り/書き込み」** に設定
8. **「共有」** ボタンを押す
9. 表示される **ネットワーク パス** をメモ。例：

   ```
   \\YOUR-PC-NAME\Users\YOU\Documents\OfficeAddins\ppt-tools
   ```

   この `\\` から始まる文字列を **コピー** してください（次のステップで貼り付けます）。

10. **「完了」** で閉じる → **「閉じる」** でプロパティを閉じる

> 💡 自分のコンピューター名は、コマンドプロンプトで `hostname` と打つか、**設定 → システム → バージョン情報** の「デバイス名」で確認できます。

### ステップ 3: PowerPoint のトラスト センターにネットワーク パスを登録

1. PowerPoint を起動
2. **ファイル → オプション**
3. 左メニューの **トラスト センター** → 右下の **トラスト センターの設定** ボタン
4. 左メニューの **信頼できるアドイン カタログ** をクリック
5. **「カタログ URL」** の入力欄に、ステップ 2-2 でコピーした **ネットワーク パス** を貼り付け：

   - ❌ `C:\OfficeAddins\ppt-tools` ← ローカルパス（エラーになります）
   - ✅ `\\YOUR-PC-NAME\Users\YOU\Documents\OfficeAddins\ppt-tools` ← `\\` から始まる UNC パス

6. **「カタログの追加」** ボタンを押す
7. 表に追加された行の **「メニューに表示」** 列のチェックボックスにチェック
8. **OK** で閉じる → もう一度 **OK** で「PowerPoint のオプション」を閉じる

### ステップ 4: PowerPoint を再起動

PowerPoint を完全に閉じてからもう一度開きます。

### ステップ 5: アドインを追加

1. **挿入** タブ → **個人用アドイン** ボタン
   （バージョンによっては「アドインの取得」→「個人用」タブ）
2. ダイアログ上部のタブで **「共有フォルダー」** を選択
3. リストに `ppt-tools` が表示されているのでクリック
4. **追加** ボタンを押す

数秒待つと **ホーム** タブの末尾に **`ppt-tools`** グループが出現します。✅ インストール完了。

## 使い方

### ホームタブのリボン

`ppt-tools` グループに 6 つのコントロールがあります：

| コントロール                    | 動作                       | 必要な選択数  |
| ------------------------------- | -------------------------- | ------------- |
| ⬇️ 下に詰める                   | 下端に向けて隙間ゼロで集約 | 2 つ以上      |
| ⬆️ 上に詰める                   | 上端に向けて集約           | 2 つ以上      |
| ⬅️ 左に詰める                   | 左端に向けて集約           | 2 つ以上      |
| ➡️ 右に詰める                   | 右端に向けて集約           | 2 つ以上      |
| 📏 高さ・幅を揃える（メニュー） | 最後に選んだ図形に合わせる | 2 つ以上      |
| 🔄 位置を入れ替える             | 2 図形の左上座標を交換     | ちょうど 2 つ |

### よく使うボタンを QAT に登録する

各ボタンを **右クリック → 「クイック アクセス ツール バーに追加」** を選ぶと、PowerPoint 上端の小さなツールバー（QAT）にピン留めできます。タブ切り替えなしで 1 クリック実行できるようになります。「高さ・幅を揃える」メニュー内の項目も個別に登録可能です。

### タスクペインから使う

リボンが手狭な場合は、**挿入 → 個人用アドイン → ppt-tools** で右側にタスクペインを開けます。同じ 7 ボタンが縦並びで表示されます。タスクペイン上部の 📌 ピン留めボタンで常駐化できます。

## アップデート

開発者がデプロイすれば、**配布先 PC は次回 PowerPoint 起動時に自動で最新版を取得** します。あなた側の手動操作は不要です。

ただし PowerPoint の WebView2 が古いキャッシュを掴むことがあります。動作が変なときは：

1. PowerPoint を完全終了
2. タスクマネージャーで `POWERPNT.exe` が残っていないことを確認
3. もう一度起動

これで大抵直ります。

## アンインストール

ステップ 3 で登録したフォルダをトラスト センターから削除：

1. **ファイル → オプション → トラスト センター → トラスト センターの設定 → 信頼できるアドイン カタログ**
2. 該当フォルダの行を選択して **削除**
3. **OK** で閉じる → PowerPoint を再起動

ステップ 2 で作ったフォルダおよび `manifest.xml` も、不要なら削除して構いません。

## トラブルシューティング

### 「https:// を指定しましたか？」というエラーが出る（カタログ URL 登録時）

ステップ 3 でローカルパス（`C:\OfficeAddins\ppt-tools` のような形式）を入力していると、このエラーが出ます。トラスト センターは `\\` から始まる **UNC パス**（ネットワーク共有パス）か、SharePoint の HTTPS URL しか受け付けません。

ステップ 2-2 でフォルダを共有した際にメモした **`\\YOUR-PC-NAME\...` で始まるネットワーク パス** を貼り付けてください。

ネットワーク パスを忘れた場合：

1. ステップ 2-1 のフォルダを **右クリック → プロパティ → 共有タブ**
2. 「ネットワーク パス」欄に表示されている `\\` 始まりの文字列をコピー

### ステップ 2-2 で「共有」ボタンが出ない／フォルダが共有できない

- Windows のファイル共有機能が無効になっている可能性があります。**設定 → ネットワークとインターネット → 共有の詳細設定** で「ネットワーク探索」と「ファイルとプリンターの共有」を有効化してください。
- 企業 PC の場合、ポリシーで共有が制限されていることがあります。情報システム部門に相談してください（または下記「上級者向け」セクションを参照）。

### 挿入タブに「共有フォルダー」が出ない

- ステップ 3 の **「メニューに表示」** チェックを忘れていませんか？
- ステップ 4（PowerPoint 再起動）を実施しましたか？

### 「ppt-tools」が共有フォルダー一覧に出ない

- `manifest.xml` のファイル名が他に変わっていないか確認（`manifest (1).xml` などになりがち）。
- フォルダの中に `manifest.xml` が **直接** 置かれているか確認（さらにサブフォルダの中ではダメ）。

### 追加してもリボンに何も出ない

- インターネット接続を確認。初回ロードでは Office.js（Microsoft 提供）と本アドインの JavaScript を取得します。
- 企業ネットワーク内なら、以下へのアクセスがファイアウォールで遮断されていないか確認：
  - `https://kentakikuchi0423.github.io`
  - `https://appsforoffice.microsoft.com`

### 「アドインを起動できませんでした」エラー

PowerPoint のアドインキャッシュ破損が原因のことが多いです。エクスプローラーのアドレスバーに以下を貼って開き、中身を全削除 → PowerPoint 再起動：

```
%LOCALAPPDATA%\Microsoft\Office\16.0\Wef\
```

### それでも解決しない

GitHub の Issue Tracker でお気軽にご相談ください：
👉 [https://github.com/kentakikuchi0423/ppt-tools/issues](https://github.com/kentakikuchi0423/ppt-tools/issues)

## 上級者向け：レジストリ経由でカタログ登録を自動化

ステップ 3 のトラスト センター登録はレジストリ書き込みで代替できます（Microsoft の[公式手順](https://learn.microsoft.com/en-us/office/dev/add-ins/testing/create-a-network-shared-folder-catalog-for-task-pane-and-content-add-ins#configure-the-trust-with-a-registry-script)）。複数 PC への展開時に便利です。

> ⚠️ ステップ 2-2 のフォルダ共有（UNC パス取得）は省略できません。レジストリエントリも UNC パスを参照する必要があります。

`TrustNetworkShareCatalog.reg` というファイルを作成し、以下を貼り付け：

```text
Windows Registry Editor Version 5.00

[HKEY_CURRENT_USER\Software\Microsoft\Office\16.0\WEF\TrustedCatalogs\{REPLACE-WITH-RANDOM-GUID}]
"Id"="{REPLACE-WITH-RANDOM-GUID}"
"Url"="\\\\YOUR-PC-NAME\\YOUR-SHARE-PATH"
"Flags"=dword:00000001
```

- `{REPLACE-WITH-RANDOM-GUID}` を 2 か所とも、新規生成した GUID で置き換え（[guidgenerator.com](https://guidgenerator.com/) など）。`{}` は残します。
- `\\\\YOUR-PC-NAME\\YOUR-SHARE-PATH` は、ステップ 2-2 でコピーした UNC パスを **バックスラッシュを 2 つに二重化** して書き直します（例: `\\PC01\ppt-tools` → `\\\\PC01\\ppt-tools`）。

PowerPoint をすべて閉じてから `.reg` ファイルをダブルクリックして実行 → 「はい」で許可。これで step 3 の Trust Center 登録は完了し、ステップ 4 へ進めます。

## ライセンス・プライバシー

- **ライセンス**: MIT License（[LICENSE](./LICENSE)）
- **プライバシーポリシー**: [PRIVACY.md](./PRIVACY.md) — 図形データ・スライド内容の収集や外部送信は **一切行いません**。すべての処理はあなたの PC 内で完結します。

---

不具合報告・機能要望・改善提案は GitHub の [Issues](https://github.com/kentakikuchi0423/ppt-tools/issues) へ。Pull Request も歓迎します。
