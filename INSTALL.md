# ppt-tools インストールガイド

PowerPoint で図形を一発整列するアドインです。所要時間 10 分。

## できること

- ⬇️⬆️⬅️➡️ 図形を下／上／左／右に **詰める**（隙間ゼロ）
- 📏 高さ・幅を **揃える**（最後に選んだ図形が基準）
- 🔄 2 つの図形の **位置を入れ替える**

すべて **Ctrl+Z** で 1 回戻せます。

## 動作環境

- Windows 10 / 11 + Microsoft 365 版 PowerPoint
- インターネット接続（初回ロード時のみ。2 回目以降はオフライン可）

## インストール手順

### ステップ 1: マニフェストファイルをダウンロード

下のリンクを **右クリック → 「名前を付けてリンク先を保存」**：

👉 [https://kentakikuchi0423.github.io/ppt-tools/manifest.xml](https://kentakikuchi0423.github.io/ppt-tools/manifest.xml)

ファイル名は `manifest.xml` のまま保存してください。

### ステップ 2: フォルダを作って共有する

エクスプローラーで好きな場所にフォルダを作成（例: `C:\OfficeAddins\ppt-tools\`）し、ステップ 1 の `manifest.xml` をその中に移動します。

そのフォルダを **右クリック → プロパティ → 共有タブ → 「共有」ボタン**：

1. ドロップダウンから自分のユーザー名を選択 → **「追加」**
2. アクセス許可を **「読み取り/書き込み」** に変更
3. **「共有」** ボタンを押す
4. 表示される **ネットワーク パス**（`\\YOUR-PC-NAME\...` で始まる UNC パス）を **コピー**

> 💡 PowerPoint はローカルパス（`C:\...`）を受け付けません。必ず `\\` から始まる UNC パスをコピーしてください。コンピューター名はコマンドプロンプトで `hostname` で確認できます。
>
> ⚠️ **スペースは `%20` ではなくそのままのスペースで** 貼り付けてください。`Custom%20Office%20Templates` のように URL エンコードされていると、PowerPoint は文字通りそのフォルダを探そうとして「カタログに接続できません」になります（URL エンコードは Web URL のルールで、UNC パスには適用されません）。

### ステップ 3: トラスト センターに UNC パスを登録

PowerPoint → **ファイル → オプション → トラスト センター → トラスト センターの設定 → 信頼できるアドイン カタログ**

「カタログ URL」にステップ 2 でコピーした UNC パスを貼り付け：

- ❌ `C:\OfficeAddins\ppt-tools`
- ✅ `\\YOUR-PC-NAME\Users\YOU\Documents\OfficeAddins\ppt-tools`

**「カタログの追加」** → 表の **「メニューに表示」** にチェック → **OK** を 2 回押して閉じる。

### ステップ 4: PowerPoint を再起動

完全に閉じてからもう一度開きます。

### ステップ 5: アドインを追加

**挿入** タブ → **個人用アドイン** → ダイアログ上部の **「共有フォルダー」** タブ → `ppt-tools` を選択 → **追加**。

ホームタブに **`ppt-tools`** グループが出現すれば完了。✅

## 使い方

ホームタブの `ppt-tools` グループに 6 つのコントロール：

| コントロール                    | 動作                       | 必要な選択数  |
| ------------------------------- | -------------------------- | ------------- |
| ⬇️ 下に詰める                   | 下端に向けて隙間ゼロで集約 | 2 つ以上      |
| ⬆️ 上に詰める                   | 上端に向けて集約           | 2 つ以上      |
| ⬅️ 左に詰める                   | 左端に向けて集約           | 2 つ以上      |
| ➡️ 右に詰める                   | 右端に向けて集約           | 2 つ以上      |
| 📏 高さ・幅を揃える（メニュー） | 最後に選んだ図形に合わせる | 2 つ以上      |
| 🔄 位置を入れ替える             | 2 図形の左上座標を交換     | ちょうど 2 つ |

ボタンを **右クリック → 「クイック アクセス ツール バーに追加」** で QAT にピン留めすれば、タブ切替なしで 1 クリック実行できます。

タスクペインで使う場合は **挿入 → 個人用アドイン → ppt-tools**。

## アップデート

開発者がデプロイすれば、配布先 PC は次回 PowerPoint 起動時に自動で最新版を取得します。手動操作は不要です。

## アンインストール

**ファイル → オプション → トラスト センター → トラスト センターの設定 → 信頼できるアドイン カタログ** で該当行を削除 → PowerPoint を再起動。

## トラブルシューティング

**「https:// を指定しましたか？」エラー（カタログ URL 登録時）**
ステップ 3 で `C:\...` のローカルパスを入れています。ステップ 2 でコピーした **`\\` から始まる UNC パス** を貼り付けてください。

**「カタログに接続できません」エラー**
パス内のスペースが `%20` に置き換わっていませんか？ Web URL のルールで、UNC パスには適用されません。`%20` はそのままの空白文字に置き換えて貼り直してください（例: `\\PC\…\Custom%20Office%20Templates` → `\\PC\…\Custom Office Templates`）。OneDrive 配下のフォルダで起きやすい場合は、OneDrive 外の単純な ASCII パス（例: `C:\OfficeAddins\ppt-tools\`）を共有し直すと安定します。

**毎回 PowerPoint 起動時にアドインを読み込み直す必要がある／QAT のピン留めが消える**
PowerPoint は起動時にカタログ URL を再スキャンしてアドインを再ロードします。これに失敗すると、ホームタブの `ppt-tools` グループも QAT のピン留め参照も無効になります。よくある原因：

- **OneDrive 配下のフォルダを共有している**: ファイル オン デマンド（クラウドのみ状態）や同期未完了で `manifest.xml` が PowerPoint 起動時に読めないことがあります。
- **共有パスに日本語・スペースが含まれる**: Office API が安定して扱えないことがあります。
- **共有が解除された**: 再起動などで Windows のファイル共有が無効化されると、パスは存在しても接続できません。

**解決**: OneDrive 外の単純な ASCII パスに移すのが最も確実です。
1. `C:\OfficeAddins\ppt-tools\` を作成し、`manifest.xml` を移動
2. プロパティ → 共有 → **詳細な共有** → 「このフォルダーを共有する」にチェック → 共有名を `ppt-tools`（半角）に
3. Trust Center で旧エントリを **削除** → 新しい UNC（例: `\\YOUR-PC\ppt-tools`）を追加 → メニューに表示にチェック
4. PowerPoint を再起動 → 挿入 → 個人用アドイン → 共有フォルダー → ppt-tools → 追加

以後は起動時に自動ロードされ、QAT のピン留めも維持されます。

**ステップ 2 で「共有」ボタンが出ない**
**設定 → ネットワークとインターネット → 共有の詳細設定** で「ネットワーク探索」と「ファイルとプリンターの共有」を有効化してください。

**「共有フォルダー」タブが表示されない**
ステップ 3 の **「メニューに表示」** チェックを忘れているか、ステップ 4（PowerPoint 再起動）が未実施です。

**`ppt-tools` が共有フォルダー一覧に出ない**
ファイル名が `manifest.xml` のままか確認（`manifest (1).xml` になっていることがあります）。フォルダ直下に置いてください（サブフォルダの中ではダメ）。

**「アドインを起動できませんでした」エラー**
キャッシュ破損です。エクスプローラーのアドレスバーに `%LOCALAPPDATA%\Microsoft\Office\16.0\Wef\` を貼って中身を全削除 → PowerPoint 再起動。

**それでも解決しない**
[GitHub Issues](https://github.com/kentakikuchi0423/ppt-tools/issues) でお気軽にご相談ください。

## ライセンス・プライバシー

- **MIT License**（[LICENSE](./LICENSE)）
- 図形データ・スライド内容の収集や外部送信は **一切行いません**（[PRIVACY.md](./PRIVACY.md)）

---

不具合・要望は [Issues](https://github.com/kentakikuchi0423/ppt-tools/issues) へ。Pull Request も歓迎します。複数 PC への一括展開には [レジストリスクリプト](https://learn.microsoft.com/en-us/office/dev/add-ins/testing/create-a-network-shared-folder-catalog-for-task-pane-and-content-add-ins#configure-the-trust-with-a-registry-script)（Microsoft 公式）も利用できます。
