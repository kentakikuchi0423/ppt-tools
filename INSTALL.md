# ppt-tools インストールガイド

PowerPoint で図形を **「揃える」「詰める」「位置入替」** を 1 クリックで実行できる無料アドインです。

このガイドは **PowerPoint で使うだけの方** 向けの手順です。プログラミング知識は不要、Node.js のインストールも不要です。所要時間は 5 分程度。

> 自分で改造したい・コードを読みたい方は [`README.md`](./README.md) を参照してください。

## できること

- ⬇️⬆️⬅️➡️ 選択した図形を下／上／左／右に **詰める**（隙間ゼロで集約）
- 📏 選択した図形の **高さ・幅を揃える**（最後に選んだ図形が基準）
- 🔄 2 つの図形の **位置を入れ替える**

すべて **Ctrl+Z** で 1 回戻せます。

## 動作環境

- Windows 10 / 11
- **Microsoft 365 版 PowerPoint**（サブスクリプション版）を推奨。Office 2019 / 2021 などの永続ライセンス版でも動く可能性が高いですが、未検証です。
- インターネット接続（初回ロード時のみ。2 回目以降はオフラインでも操作可能）

Mac 版・Web 版 PowerPoint も理論上は動作しますが、本ガイドは Windows 向けです。

## インストール手順（5 ステップ・約 5 分）

### ステップ 1: マニフェストファイルをダウンロード

下のリンクを **右クリック → 「名前を付けてリンク先を保存」**：

👉 [https://kentakikuchi0423.github.io/ppt-tools/manifest.xml](https://kentakikuchi0423.github.io/ppt-tools/manifest.xml)

ファイル名は `manifest.xml` のままにしてください。

### ステップ 2: 専用フォルダに置く

エクスプローラーで好きな場所にフォルダを作り（例: `C:\OfficeAddins\ppt-tools\`）、ステップ 1 で保存した `manifest.xml` をその中に移動します。

> このフォルダはアドインを使う間ずっと存在し続ける必要があります。ダウンロードフォルダや一時フォルダではなく、消さない場所を選んでください。

### ステップ 3: PowerPoint のトラスト センターにフォルダを登録

1. PowerPoint を起動
2. **ファイル → オプション**
3. 左メニューの **トラスト センター** → 右下の **トラスト センターの設定** ボタン
4. 左メニューの **信頼できるアドイン カタログ** をクリック
5. **「カタログ URL」** の入力欄にステップ 2 のフォルダパスを入力
   例: `C:\OfficeAddins\ppt-tools`
6. **「カタログの追加」** ボタンを押す
7. 表に追加された行の **「メニューに表示」** 列のチェックボックスにチェック
8. **OK** で閉じる → もう一度 **OK** で「PowerPoint のオプション」を閉じる

### ステップ 4: PowerPoint を再起動

PowerPoint を完全に閉じてからもう一度開きます。

### ステップ 5: アドインを追加

1. **挿入** タブ → **個人用アドイン** ボタン
   （バージョンによっては「アドインの取得」→「個人用」タブ）
2. ダイアログ上部のタブで **「共有フォルダー」** を選択
3. リストに `ppt-tools` が表示されているのでクリック
4. **追加** ボタンを押す

数秒待つと **ホーム** タブの末尾に **`ppt-tools`** グループが出現します。✅ インストール完了。

## 使い方

### ホームタブのリボン

`ppt-tools` グループに 6 つのコントロールがあります：

| コントロール | 動作 | 必要な選択数 |
| --- | --- | --- |
| ⬇️ 下に詰める | 下端に向けて隙間ゼロで集約 | 2 つ以上 |
| ⬆️ 上に詰める | 上端に向けて集約 | 2 つ以上 |
| ⬅️ 左に詰める | 左端に向けて集約 | 2 つ以上 |
| ➡️ 右に詰める | 右端に向けて集約 | 2 つ以上 |
| 📏 高さ・幅を揃える（メニュー） | 最後に選んだ図形に合わせる | 2 つ以上 |
| 🔄 位置を入れ替える | 2 図形の左上座標を交換 | ちょうど 2 つ |

### よく使うボタンを QAT に登録する

各ボタンを **右クリック → 「クイック アクセス ツール バーに追加」** を選ぶと、PowerPoint 上端の小さなツールバー（QAT）にピン留めできます。タブ切り替えなしで 1 クリック実行できるようになります。「高さ・幅を揃える」メニュー内の項目も個別に登録可能です。

### タスクペインから使う

リボンが手狭な場合は、**挿入 → 個人用アドイン → ppt-tools** で右側にタスクペインを開けます。同じ 7 ボタンが縦並びで表示されます。タスクペイン上部の 📌 ピン留めボタンで常駐化できます。

## アップデート

開発者がデプロイすれば、**配布先 PC は次回 PowerPoint 起動時に自動で最新版を取得** します。あなた側の手動操作は不要です。

ただし PowerPoint の WebView2 が古いキャッシュを掴むことがあります。動作が変なときは：

1. PowerPoint を完全終了
2. タスクマネージャーで `POWERPNT.exe` が残っていないことを確認
3. もう一度起動

これで大抵直ります。

## アンインストール

ステップ 3 で登録したフォルダをトラスト センターから削除：

1. **ファイル → オプション → トラスト センター → トラスト センターの設定 → 信頼できるアドイン カタログ**
2. 該当フォルダの行を選択して **削除**
3. **OK** で閉じる → PowerPoint を再起動

ステップ 2 で作ったフォルダおよび `manifest.xml` も、不要なら削除して構いません。

## トラブルシューティング

### 挿入タブに「共有フォルダー」が出ない

- ステップ 3 の **「メニューに表示」** チェックを忘れていませんか？
- ステップ 4（PowerPoint 再起動）を実施しましたか？

### 「ppt-tools」が共有フォルダー一覧に出ない

- `manifest.xml` のファイル名が他に変わっていないか確認（`manifest (1).xml` などになりがち）。
- フォルダの中に `manifest.xml` が **直接** 置かれているか確認（さらにサブフォルダの中ではダメ）。

### 追加してもリボンに何も出ない

- インターネット接続を確認。初回ロードでは Office.js（Microsoft 提供）と本アドインの JavaScript を取得します。
- 企業ネットワーク内なら、以下へのアクセスがファイアウォールで遮断されていないか確認：
  - `https://kentakikuchi0423.github.io`
  - `https://appsforoffice.microsoft.com`

### 「アドインを起動できませんでした」エラー

PowerPoint のアドインキャッシュ破損が原因のことが多いです。エクスプローラーのアドレスバーに以下を貼って開き、中身を全削除 → PowerPoint 再起動：

```
%LOCALAPPDATA%\Microsoft\Office\16.0\Wef\
```

### それでも解決しない

GitHub の Issue Tracker でお気軽にご相談ください：
👉 [https://github.com/kentakikuchi0423/ppt-tools/issues](https://github.com/kentakikuchi0423/ppt-tools/issues)

## ライセンス・プライバシー

- **ライセンス**: MIT License（[LICENSE](./LICENSE)）
- **プライバシーポリシー**: [PRIVACY.md](./PRIVACY.md) — 図形データ・スライド内容の収集や外部送信は **一切行いません**。すべての処理はあなたの PC 内で完結します。

---

不具合報告・機能要望・改善提案は GitHub の [Issues](https://github.com/kentakikuchi0423/ppt-tools/issues) へ。Pull Request も歓迎します。
